# Hail 1.10.0 二次开发更新说明

更新日期：2026-07-28  
二次开发：野生的烧烤大师

本文档整理当前源码相对 Hail 1.10.0 原版的二次开发内容、使用方式、构建方式与注意事项。

## 一、核心改动概览

本次二次开发主要围绕三个方向：

- 支持多用户、工作资料、多开应用的读取与冻结操作。
- 新增桌面“大文件夹”小组件，用于快速解冻并启动冻结名单中的应用。
- 在关于页增加二次开发信息与微信公众号关注引导。

## 二、多用户 / 工作资料 / 多开应用支持

原版 Hail 主要按 `packageName` 识别应用，因此同一个应用在主用户、工作资料或多开用户中会被视为同一个应用。

本次修改将应用身份升级为：

```text
packageName + userId
```

相关能力：

- 应用数据 `AppInfo` 新增 `userId`。
- 首页冻结名单保存时新增 `user` 字段。
- 应用勾选、删除、冻结状态判断按 `packageName + userId` 区分。
- Root / Shizuku 模式下冻结、解冻、停止、停用、隐藏、暂停、卸载、重装等操作支持目标 `userId`。
- API / 快捷方式 / 延迟任务支持携带 `user` 参数。
- 应用列表尝试通过 `LauncherApps` 读取工作资料 / 多开用户可见应用。

注意：

- Android 普通应用权限无法完整读取所有用户的所有包。
- 对工作资料、多开用户的完整操作更依赖 Root 或 Shizuku 权限。
- 部分桌面、多开框架或系统 ROM 可能限制跨用户启动行为。

## 三、桌面大文件夹小组件

新增标准 Android AppWidget 小组件：

```text
Hail folder / 雹文件夹
```

兼容目标：

- Android 原生桌面小组件体系。
- HyperOS、ColorOS 等基于标准 AppWidget 的桌面。

### 3.1 添加方式

安装新版 APK 后：

1. 回到桌面。
2. 长按桌面空白处。
3. 进入“小组件 / 插件 / 添加小部件”。
4. 找到 `Hail` 或 `Hail folder / 雹文件夹`。
5. 添加后进入配置页。
6. 勾选要显示在小组件里的应用。

如果列表中暂时找不到：

- 先卸载旧版再安装新版。
- 删除旧小组件后重新添加。
- 在桌面小组件搜索里搜索 `Hail`。
- 某些国产桌面可能需要重启桌面或重启手机后刷新小组件列表。

### 3.2 小组件可配置项

小组件配置页支持：

- 仅显示 Hail 首页冻结名单里的应用。
- 自定义文件夹标题。
- 标题留空时隐藏标题栏文字。
- 图标大小无级调节，范围：

```text
48dp - 96dp
```

- 设置是否显示应用名称。
- 设置背景透明度。
- 勾选要显示在当前小组件内的应用。

### 3.3 小组件交互

小组件内应用图标：

- 点击应用图标：先解冻，再启动应用。
- 已解冻应用显示彩色图标。
- 已冻结应用显示灰色图标。

右下角固定按钮：

- 小组件右下角悬浮显示“一键冻结”按钮。
- 滑动应用图标列表时，该按钮保持不动。
- 点击后冻结当前小组件勾选的所有应用。

布局说明：

- 应用图标网格会正常铺满宽度。
- 一键冻结按钮不再占用整列空间，只是悬浮覆盖在右下角。

## 四、关于页二次开发信息

在“关于”页面的版本信息下方新增：

```text
二次开发：野生的烧烤大师
点击关注微信公众号
```

点击后弹窗：

标题：

```text
公众号
```

内容：

```text
野生的烧烤大师公众号已经复制到粘贴板，打开微信搜索即可
```

按钮：

- `稍后再说`：关闭弹窗。
- `去关注`：复制 `野生的烧烤大师` 到剪贴板，然后打开微信。

采用该方案的原因：

- 微信公众号主页链接在部分系统上会被浏览器接管。
- 强制指定微信打开链接在部分机型 / 微信版本中仍可能失败。
- 复制公众号名称并打开微信搜索，是当前更稳定的引导方式。

## 五、构建说明

当前环境使用 Android Studio 自带 JDK：

```bat
D:\APPSoftware\android-studio\jbr
```

构建 release APK：

```bat
cd /d "D:\GPT project\Android\Hail-1.10.0"
set JAVA_HOME=D:\APPSoftware\android-studio\jbr
set PATH=D:\APPSoftware\android-studio\jbr\bin;%PATH%
gradlew.bat assembleRelease
```

构建产物位置：

```text
app\build\outputs\apk\release\Hail-v1.10.0-gnogit.apk
```

当前已验证：

```text
BUILD SUCCESSFUL
```

## 六、源码包兼容改动

由于当前源码来自 GitHub 下载包，不一定包含 `.git` 目录，原版 Gradle 脚本中读取 Git 提交号会失败。

已修改：

- 没有 Git 仓库时使用 `nogit` 作为版本后缀。
- 避免 `git rev-parse` 失败导致无法构建。

因此当前生成的 APK 文件名包含：

```text
gnogit
```

## 七、主要涉及文件

多用户 / 多开支持：

- `app/src/main/kotlin/com/aistra/hail/app/AppInfo.kt`
- `app/src/main/kotlin/com/aistra/hail/app/HailData.kt`
- `app/src/main/kotlin/com/aistra/hail/app/AppManager.kt`
- `app/src/main/kotlin/com/aistra/hail/app/HailApi.kt`
- `app/src/main/kotlin/com/aistra/hail/utils/HPackages.kt`
- `app/src/main/kotlin/com/aistra/hail/utils/HShell.kt`
- `app/src/main/kotlin/com/aistra/hail/utils/HShizuku.kt`
- `app/src/main/kotlin/com/aistra/hail/ui/apps/*`
- `app/src/main/kotlin/com/aistra/hail/ui/home/*`
- `app/src/main/kotlin/com/aistra/hail/ui/api/ApiActivity.kt`
- `app/src/main/kotlin/com/aistra/hail/work/*`

桌面小组件：

- `app/src/main/kotlin/com/aistra/hail/widgets/HailFolderWidgetProvider.kt`
- `app/src/main/kotlin/com/aistra/hail/widgets/HailFolderWidgetService.kt`
- `app/src/main/kotlin/com/aistra/hail/widgets/HailFolderWidgetStore.kt`
- `app/src/main/kotlin/com/aistra/hail/widgets/HailFolderWidgetConfigureActivity.kt`
- `app/src/main/res/xml/hail_folder_widget.xml`
- `app/src/main/res/layout/widget_hail_folder.xml`
- `app/src/main/res/layout/item_widget_app.xml`
- `app/src/main/res/drawable/bg_widget_folder.xml`
- `app/src/main/res/drawable/bg_widget_freeze.xml`

关于页二次开发信息：

- `app/src/main/kotlin/com/aistra/hail/ui/about/AboutFragment.kt`
- `app/src/main/res/values/strings.xml`
- `app/src/main/res/values-zh-rCN/strings.xml`

构建兼容：

- `app/build.gradle.kts`

## 八、测试建议

建议测试以下场景：

- 主用户应用是否正常显示、冻结、解冻、启动。
- 工作资料 / 多开用户应用是否能显示。
- 工作资料 / 多开用户应用是否能从 Hail 首页启动。
- 工作资料 / 多开用户应用是否能从小组件解冻启动。
- 小组件标题为空时是否隐藏。
- 小组件图标大小 `48dp - 96dp` 调节是否生效。
- 关闭应用名称后，小组件是否只显示图标。
- 一键冻结按钮是否固定在右下角。
- 滚动小组件应用列表时，一键冻结按钮是否保持不动。
- 已冻结应用是否灰色，解冻后是否恢复彩色。
- 关于页公众号弹窗、复制剪贴板、打开微信是否正常。

## 九、已知限制

- 桌面小组件由系统 Launcher 托管，不同 ROM 对 AppWidget 刷新、透明度、列表滚动、点击事件的兼容性可能不同。
- RemoteViews 小组件不能像普通应用界面那样完全自由布局，部分效果采用兼容性优先的实现方式。
- 跨用户应用读取与操作受 Android 系统权限限制，普通权限下不能保证完整覆盖所有用户。
- Shizuku adb 模式无法执行部分隐藏应用相关能力，Root / Shizuku root 更完整。
